import styled from "styled-components";

export const DescriptionWrapper = styled.div`
  margin-top: 30px;

  h3 {
    font-size: 14px;
    font-weight: 600;
  }
  strong {
    font-size: 16px;
    font-weight: 700;
    color: var(--primary);
    /* font-family: serif; */
  }
  p {
    color: var(--black);
  }
`;
