import styled from "styled-components";

export const HeaderWrapper = styled.div`
  .container_check_shipment_cost .anticon {
    color: var(--dark-green);
    font-weight: bold;
  }

  .container_jumbotron {
    margin-bottom: 15px;
    margin-top: 77px;
  }

  .component_content {
    padding-top: 90px;
    font-family: serif;
    font-size: 21px;
    line-height: 1.58;
    color: rgba(0, 0, 0, 0.84);
  }
`;
