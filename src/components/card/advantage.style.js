import styled from 'styled-components';

export const AdvantageWrapper = styled.div`
  .component_advantage_image {
    margin-bottom: 10px;
  }

  .component_advantage_title {
    font-size: 20px;
    line-height: 150%;
    text-align: center;
    color: var(--black-two);
    font-weight: bold;
    margin-bottom: 3px;
  }

  .component_advantage_description {
    font-size: 18px;
    line-height: 150%;
    color: var(--grey);
    width: 80%;
    margin: auto;
    text-align: center;
  }
`;
