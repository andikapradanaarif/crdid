import styled from "styled-components";

export const CardWrapper = styled.div`
  .component_card_wrapper {
    /* background-color: red; */
    /* background-color: var(--primary); */
  }
  h2 {
    padding-bottom: 2px;
    font-size: 16px;
    line-height: 26px;
    font-weight: 600;
    color: var(--primary);
  }
`;
